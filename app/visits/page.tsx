export const dynamic='force-dynamic';
export const runtime='nodejs';
import { prisma } from '../../lib/prisma';

export default async function VisitsPage({searchParams}:{searchParams?:Promise<{q?:string;type?:string}>}){const params=(await searchParams)??{};const q=(params.q??'').trim();const type=(params.type??'').trim();const visits=await prisma.loggedVisit.findMany({take:300,where:{locationType:type||undefined,OR:q?[{summary:{contains:q,mode:'insensitive'}},{outcomes:{contains:q,mode:'insensitive'}},{nextStep:{contains:q,mode:'insensitive'}}]:undefined},orderBy:{visitAt:'desc'}});const agencies=await prisma.agency.findMany({where:{id:{in:visits.map(v=>v.agencyId).filter(Boolean) as string[]}}});const wholesale=await prisma.wholesaleAccount.findMany({where:{id:{in:visits.map(v=>v.wholesaleAccountId).filter(Boolean) as string[]}}});const contacts=await prisma.locationContact.findMany({where:{id:{in:visits.map(v=>v.contactId).filter(Boolean) as string[]}}});const agencyMap=Object.fromEntries(agencies.map(a=>[a.id,a.name]));const wholesaleMap=Object.fromEntries(wholesale.map(a=>[a.id,a.name]));const contactMap=Object.fromEntries(contacts.map(c=>[c.id,c.name]));return<><h1>Visits</h1><form method='get' className='card' style={{display:'grid',gridTemplateColumns:'2fr 1fr auto',gap:8,maxWidth:920}}><input name='q' defaultValue={q} placeholder='Filter notes/outcomes/next step'/><select name='type' defaultValue={type}><option value=''>All types</option><option value='agency'>Agency</option><option value='wholesale'>Wholesale</option></select><button type='submit'>Filter</button></form><table><thead><tr><th>Date</th><th>Type</th><th>Location</th><th>Contact</th><th>Summary</th><th>Next Step</th></tr></thead><tbody>{visits.map(v=><tr key={v.id}><td>{new Date(v.visitAt).toLocaleString()}</td><td>{v.locationType}</td><td>{v.locationType==='agency'?agencyMap[v.agencyId??'']:wholesaleMap[v.wholesaleAccountId??'']}</td><td>{contactMap[v.contactId??'']}</td><td>{v.summary}</td><td>{v.nextStep}</td></tr>)}</tbody></table></>}
